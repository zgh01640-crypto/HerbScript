# HerbScript 前端界面方案

## 1. 设计目标

HerbScript 的前端不是通用后台模板，而是一个带有中医处方语义的业务系统界面。整体设计目标如下：

- 保持后台系统的清晰、高效和易操作
- 避免默认管理后台的“灰白表格感”
- 突出“本草、纸方、校对、留痕”这几个核心体验关键词
- 重点强化图片识别校对页的工作台感

## 2. 视觉方向

### 2.1 风格关键词

- 温润
- 克制
- 专业
- 有东方草木气质
- 强调信息秩序

### 2.2 色彩方案

主色不走常见紫色或纯蓝色后台路线，采用“药柜木色 + 草本绿 + 米白纸感”的组合：

- 主色：`#2f6b57`
- 主色深色：`#234f41`
- 辅助色：`#c58a49`
- 背景色：`#f6f1e8`
- 卡片底色：`#fffdf8`
- 线条色：`#d8cfbf`
- 正文色：`#2b2b2b`
- 次级文字：`#746b5f`
- 危险色：`#b64b43`

### 2.3 字体建议

优先使用：

- 中文标题：`"Noto Serif SC", "Source Han Serif SC", serif`
- 正文与表单：`"PingFang SC", "Microsoft YaHei", sans-serif`
- 数字与编码：`"JetBrains Mono", monospace`

## 3. 页面布局策略

### 3.1 登录页

采用左右分栏：

- 左侧为品牌说明与系统定位
- 右侧为登录卡片

视觉重点：

- 使用米白纸纹渐变背景
- 加入药柜式网格或纸张投影感
- 登录卡片像“处方夹板”

### 3.2 主布局

- 左侧深色导航栏
- 顶部浅色工具栏
- 主内容区使用柔和卡片与留白
- 关键数字卡片采用颜色分层，不用纯蓝统计卡

### 3.3 识别校对页

这是产品最关键的页面，采用双栏工作台：

- 左栏：处方原图、缩放与预览
- 右栏：结构化字段和药味编辑

重点体验：

- 低置信字段高亮
- 药味编辑表格固定表头
- 顶部显示识别状态、模型来源、提醒信息

## 4. 页面清单

### 4.1 登录页

包含：

- 品牌标题
- 系统说明
- 用户名密码输入
- 登录按钮

### 4.2 控制台

包含：

- 今日新增
- 待校对
- 已完成
- 最近处方
- 快捷入口

### 4.3 处方列表页

包含：

- 筛选栏
- 数据表格
- 状态标签
- 新增处方按钮
- 图片识别录入按钮

### 4.4 图片识别校对页

包含：

- 原图预览面板
- 结构化字段表单
- 药味明细表格
- 低置信提示
- 保存草稿与确认入库按钮

### 4.5 处方详情页

包含：

- 处方摘要
- 患者信息
- 处方基础信息
- 药味明细
- 操作记录

## 5. 组件策略

优先复用以下组件：

- `AppShell`
- `StatCard`
- `SectionCard`
- `StatusPill`
- `ConfidenceBadge`
- `PrescriptionItemEditor`

## 6. 工具选择说明

本轮实现采用“直接编码落地”的方式，不依赖 `frontdesign` 出静态稿。

原因如下：

- 当前环境中没有可直接调用的 `frontdesign` 工具
- 这个项目更适合先产出真实可运行页面，而不是停留在静态设计稿
- 代码实现后可以立即联动接口、表单与业务流

如果后续你已经有 `frontdesign` 的视觉稿，我们也可以再对照迭代前端样式。
